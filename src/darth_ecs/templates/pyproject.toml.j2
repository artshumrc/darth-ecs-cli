[project]
name = "{{ project_name }}-infra"
version = "0.1.0"
description = "CDK infrastructure for {{ project_name }} — generated by darth-ecs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aws-cdk-lib>=2.170.0",
    "boto3>=1.35",
    "constructs>=10.4",
]
