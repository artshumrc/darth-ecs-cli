# {{ project_name }} Infrastructure

Generated by [darth-ecs](https://github.com/artshumrc/infrastructure/tree/main/darthecs).

## Quick Start

```bash
# Deploy production
darth-ecs deploy --env prod

# Deploy a feature environment
darth-ecs deploy --env feature-xyz

# Tail logs
darth-ecs logs {{ services[0].name }} --env prod -f

# Exec into container
darth-ecs exec {{ services[0].name }} --env prod

# Destroy a non-prod environment
darth-ecs destroy --env feature-xyz
```

## Configuration

Edit `darth-ecs.toml` to modify project settings. To add a new environment,
append the name to the `environments` list and deploy:

```toml
[project]
environments = ["prod", "dev", "my-new-env"]
```

```bash
darth-ecs deploy --env my-new-env
```

## Manual CDK Usage

If you need to use CDK directly:

```bash
uv run cdk synth
uv run cdk deploy
uv run cdk diff
```
